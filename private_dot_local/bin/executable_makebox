#!/bin/bash

exec orb create debian:trixie ${1:-box} \
  --isolated \
  --mount "$HOME/projects/code:/code" \
  --mount "$HOME/projects/docs:/docs" \
  -c $HOME/.local/share/orbstack/cloud-config.yaml
